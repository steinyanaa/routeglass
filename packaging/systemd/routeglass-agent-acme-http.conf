# Install as /etc/systemd/system/routeglass-agent.service.d/acme-http.conf only
# when the Agent is the standalone HTTP-01 listener on TCP/80.
[Service]
CapabilityBoundingSet=CAP_NET_RAW CAP_NET_BIND_SERVICE
AmbientCapabilities=CAP_NET_RAW CAP_NET_BIND_SERVICE

